<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Change Email</title>
    <!-- Include Bootstrap CSS for styling -->
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
    <style>
        body {
            background-color: #f8f9fa;
        }

        .card {
            border: none;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            margin-top: 50px;
        }

        .card-header {
            background-color: #007bff;
            color: #fff;
            font-size: 1.25rem;
            font-weight: bold;
            text-align: center;
        }

        .form-control {
            border-radius: 0.25rem;
        }

        .btn-submit {
            background-color: #007bff;
            color: white;
            border: none;
            border-radius: 0.25rem;
        }

        .btn-submit:hover {
            background-color: #0056b3;
        }

        .text-muted {
            font-size: 0.9rem;
        }

        .required {
            color: red;
        }
    </style>
</head>

<body>
    <div class="container">
        <div class="row justify-content-center">
            <div class="col-md-6">
                <div class="card">
                    <div class="card-header">
                        Change Email Address
                    </div>
                    <div class="card-body">
                        <p class="text-center text-muted">
                            Enter your new email address below to update your account.
                        </p>
                        <form method="POST" action="/updateEmail">
                            <!-- New Email Address -->
                            <div class="form-group">
                                <label for="newEmail">New Email Address <span class="required">*</span></label>
                                <input required type="email" class="form-control" id="newEmail" name="newEmail"
                                    placeholder="Enter your new email address">
                            </div>
                            <!-- Confirm New Email Address -->
                            <div class="form-group">
                                <label for="confirmEmail">Confirm New Email Address <span class="required">*</span></label>
                                <input required type="email" class="form-control" id="confirmEmail" name="confirmEmail"
                                    placeholder="Re-enter your new email address">
                            </div>
                            <!-- Submit Button -->
                            <button type="submit" class="btn btn-submit btn-block">
                                Update Email
                            </button>
                        </form>
                        <hr>
                        <p class="text-center">
                            <a href="/profile" class="text-primary">Back to Account Settings</a>
                        </p>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- Include Bootstrap JS and jQuery -->
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@4.5.2/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>
